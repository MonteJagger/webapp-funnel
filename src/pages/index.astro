---

import Hero, { type HeroContentModel } from '../components/Hero/Hero';
import Layout from '../layouts/Layout.astro';

// TODO: add hero content type
const { frontmatter } = Astro.props;
---



<Layout title="Signup to FloSports">
	<main>
		<!-- Hero -->
		 <Hero content{...frontmatter.content} />
	</main>
</Layout>
